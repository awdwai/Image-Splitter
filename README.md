# AnimAI (Image-Splitter)

Scaffold for **AnimAI** — an AI image-decomposition platform. This repository holds two products:

| Product | Path | Role |
|---------|------|------|
| **Backend** | [`backend/`](backend/) | Reusable FastAPI API (primary product) |
| **Frontend** | [`frontend/`](frontend/) | Example web client that talks to the API |

Full setup and run instructions will land as the scaffold is completed. For API-only use, see [`backend/README.md`](backend/README.md).
